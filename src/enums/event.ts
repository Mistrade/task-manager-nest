export enum EEventPriorities {
  VERY_HIGH = 'veryHigh',
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
  VERY_LOW = 'veryLow',
}

export enum EEventStatuses {
  PLANNING = 'planning',
  IN_PROGRESS = 'in_progress',
  CHECK = 'check',
  DONE = 'done',
  ARCHIVE = 'archive',
}
